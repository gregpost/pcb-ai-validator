{
  "name": "pcb-ai-validator",
  "version": "1.0.0",
  "description": "Intelligent debugging assistant for Altium Designer PCB projects",
  "scripts": {
    "dev": "npm run build:fe && tsx server.ts",
    "build": "npm run build:fe && echo 'No build step required for this full-stack app'",
    "build:fe": "esbuild frontend/sidebar/sidebar.ts --bundle --outfile=frontend/sidebar/sidebar.js && esbuild frontend/sidebar/file_dialog/file_dialog.ts --bundle --outfile=frontend/sidebar/file_dialog/file_dialog.js",
    "lint": "echo 'No linting needed for this full-stack app'",
    "start": "npm run build:fe && tsx server.ts"
  },
  "dependencies": {
    "esbuild": "^0.28.0",
    "express": "^4.21.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
